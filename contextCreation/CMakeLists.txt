CMAKE_MINIMUM_REQUIRED(VERSION 2.8.9)

INCLUDE_DIRECTORIES(${GLFW_INCLUDE_DIRS})

ADD_EXECUTABLE(contextCreation main.c)

TARGET_LINK_LIBRARIES(contextCreation ${EXTRA_LIBS} )
TARGET_LINK_LIBRARIES(contextCreation ${GLFW_LIBRARIES})
TARGET_LINK_LIBRARIES(contextCreation ${GLEW_LIBRARY})