CMAKE_MINIMUM_REQUIRED(VERSION 2.8.9)

INCLUDE_DIRECTORIES(${GLFW_INCLUDE_DIRS})

ADD_EXECUTABLE(geometryShaders main.cpp)

TARGET_LINK_LIBRARIES(geometryShaders ${GLFW_LIBRARIES})
TARGET_LINK_LIBRARIES(geometryShaders ${GLEW_LIBRARY})
TARGET_LINK_LIBRARIES(geometryShaders ${EXTRA_LIBS} )